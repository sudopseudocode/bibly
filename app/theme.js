import { createMuiTheme } from '@material-ui/core/styles';

const theme = createMuiTheme({
  palette: {
    common: {
      lightGray: '#C1C1C1',
      darkGray: '#969696',
    },
    primary: {
      light: '#7D0BD6',
      main: '#7D0BD6',
      dark: '#4614B0',
    },
  },
});

export default theme;
